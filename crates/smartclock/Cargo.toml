[package]
name = "smartclock"
description = "Library for HP / Symmetricom SmartClock GPS time and frequency receivers"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true

[lints]
workspace = true

[build-dependencies]
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.6"

[dependencies]
thiserror = "2.0.20"
